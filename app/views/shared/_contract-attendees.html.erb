<% if @contract.attendees %>
  <div class="attendees-form">
    <% @contract.attendees.each_with_index do |attendee, index| %>
      <div class="form-for-attendees">
        <div class="form-group string required contract_attendees_first_name form-group-valid">
          <input class="form-control is-valid string required" type="text" value="<%= attendee["first_name"] %>" name="contract[attendees][<%= index %>][first_name]" id="contract_attendees_<%= index %>_first_name" placeholder="Prénom(s)">
        </div>
        <div class="form-group string required contract_attendees_last_name form-group-valid">
          <input class="form-control is-valid string required" type="text" value="<%= attendee["last_name"] %>" name="contract[attendees][<%= index %>][last_name]" id="contract_attendees_<%= index %>_last_name" placeholder="Nom">
        </div>
        <div class="form-group string required contract_attendees_position form-group-valid">
          <input class="form-control is-valid string required" type="text" value="<%= attendee["position"] %>" name="contract[attendees][<%= index %>][position]" id="contract_attendees_<%= index %>_position" placeholder="Fonction">
        </div>
      </div>
    <% end %>
  </div>
<% else %>
  <div id="attendees-form">
  </div>
<% end %>
